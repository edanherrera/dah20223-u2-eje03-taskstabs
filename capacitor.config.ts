import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dah20223-u2-eje03-taskstabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
